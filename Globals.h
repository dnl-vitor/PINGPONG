#pragma once
#include <raylib.h>

// 'inline' permite definir e inicializar diretamente no header (C++17)
inline int player1_score = 0;
inline int player2_score = 0;

inline Color cobaltBlue = Color{0, 71, 171, 255};
inline Color darkBlue   = Color{0, 0, 139, 255};
inline Color yellow     = Color{243, 213, 91, 255};
